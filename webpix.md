How does webpix work??

It wants to make or find webpix and my_images in something called imageDrop (or Drop, slightly deprecated, but still defined in unix.mk). Best to use local.mk to define imageDrop.
